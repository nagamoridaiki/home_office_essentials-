# ルートをビルドコンテキストにするビルド（web）で、Docker に送らないもの

# 依存とビルド結果。macOS 用のファイルを含むため、コンテナ内で作り直す
**/node_modules
**/.next

# シークレット。イメージに焼き込まないようにする
**/.env
**/.env.*

# web のビルドに不要なもの
.git
backend
.claude
.specify
.vscode
